# frontend

Angular scaffold placeholder.

Suggested next steps:
- `npm install -g @angular/cli`
- `npm install`
- add Keycloak init in `main.ts` / an auth service
- point API calls to `http://localhost:8080`

